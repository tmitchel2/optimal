# Optimal

A .NET 10.0 library project written in C#. The project uses strict code quality standards with Roslynator analyzers and treats warnings as errors.

## Optimal Project Structure

- **Solution**: `src/Optimal.slnx`
- **Main Project**: `src/Optimal/Optimal.csproj`
